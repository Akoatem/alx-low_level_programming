#include <stdio.h>

/**
 * main - print alphabet both lower and uppercase
 * Description: Alphabet letters
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i, j;

	i = 97;
	j = 65;

	while (1 < 123)
	{
		putchar(i);
		i++;
	}
	while (j < 91)
	{
		putchar(j);
		j++
	}
	putchar(10);
	return (0);
}
