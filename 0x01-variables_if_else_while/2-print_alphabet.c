#include <stdio.h>

/**
 * msin - To print number in alphabet
 * Description: print in lowercase letters
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	return (0);
}
