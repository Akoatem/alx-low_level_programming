#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: result (product of two integers)
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
